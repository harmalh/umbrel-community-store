map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

server {
    listen 80;
    server_name _;
    resolver 127.0.0.11 ipv6=off valid=30s;

    location = /bootstrap-entry {
        return 302 /bootstrap-entry/;
    }

    location /bootstrap-entry/ {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /bootstrap-entry/index.html;
        add_header Cache-Control "no-store";
    }

    location = /__paperclip_invite {
        alias /app-data/PAPERCLIP_FIRST_ADMIN_INVITE.txt;
        default_type text/plain;
        add_header Cache-Control "no-store";
    }

    location / {
        set $paperclip_upstream http://web:3100;
        proxy_pass $paperclip_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}
